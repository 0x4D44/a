# Journal: Saved Exit Codes Implementation

**Date**: 2026-03-14
**Design**: `wrk_docs/2026.03.13 - HLD - Saved Exit Codes in Chain Commands - V3.md`
**Repo**: `/home/md/language/a`

## Implementation Plan

| Stage | Description | Status |
|-------|-------------|--------|
| 1 | Data model + utility functions + tests | Done |
| 2 | CLI parsing (--save, --if-saved) + validation + tests | Done |
| 3 | Execution changes (execute_sequential_chain) + tests | Done |
| 4 | Display changes + help text + tests | Done |
| 5 | Version bump + cargo checks | Done |
| 6 | Code review + devil's advocate + apply feedback | Done |
| 7 | Completeness check vs design | Done |

## Commits

| Hash | Stage | Description |
|------|-------|-------------|
| be13465 | 1 | feat(chain): add IfSaved operator and save_as data model |
| dce5bb8 | 2 | feat(chain): add --save and --if-saved CLI parsing with validation |
| 8b8ff39 | 3 | feat(chain): implement saved exit code execution logic |
| 8bb5bbd | 4 | feat(chain): update display and help text for save/if-saved |
| 851071c | 5 | chore: bump version to 1.6.0 for saved exit codes feature |
| 9b767f0 | 6 | fix(chain): apply review feedback — fix tests and remove dead code |

## Test Coverage

- 119 unit tests + 53 integration tests = 172 total (all passing)
- All 18 test plan items from design covered
- Pre-existing test compilation bug fixed (type inference in `get_status`)

## Log

### 2026-03-14 — Starting implementation
- Design reviewed (V3 final, no critical issues)
- All work in `src/main.rs` (single-file binary crate)
- Fixed pre-existing test compilation error (type inference in `get_status` helper)
- Tollens exit code change is a separate task (different repo)

### 2026-03-14 — Stage 1: Data model
- Added `save_as: Option<String>` to `ChainCommand` (46 construction sites updated)
- Added `IfSaved { name, code }` to `ChainOperator` (all match arms updated)
- Added `is_valid_save_name` and `parse_name_code` utility functions
- 11 new tests (serialization, name validation, parse_name_code)

### 2026-03-14 — Stage 2: CLI parsing
- Added `--save <name>` backward-looking modifier
- Added `--if-saved <name>=<code> <command>` operator
- Parse-time validation: forward refs rejected, parallel+save rejected
- Simple→Chain collapse prevented when save_as set
- 5 new tests

### 2026-03-14 — Stage 3: Execution
- `HashMap<String, i32>` tracks saved codes in `execute_sequential_chain`
- Replaced IfSaved placeholder with actual lookup
- Skip messages: "was N, expected M" and "not set"
- `[saving as 'name']` annotation on execution output
- 5 new tests (match/mismatch, skipped save, last_exit_code, duplicates)

### 2026-03-14 — Stage 4: Display + help
- `command_display()`: `cmd@name` for save_as, `?s[name=code]` for IfSaved
- Info view: plain English save descriptions
- Help text: --save and --if-saved entries added
- 2 new tests

### 2026-03-14 — Stage 5: Version bump
- 1.5.0 → 1.6.0 (minor: new feature)

### 2026-03-14 — Stage 6: Review + fixes
- Devil's advocate review found: missing parallel+save test, missing e2e test, test condition mismatch, stale allow(dead_code)
- All feedback applied: 3 new tests added, test condition fixed, dead code annotation removed

### 2026-03-14 — Stage 7: Completeness check
- 36/36 design requirements PASS (100% complete)
- One note: `--save` without preceding command error path is defensive/unreachable (parser always pushes first command before option loop)

## Remaining Work (separate tasks)
- **Tollens**: `tollens stop` must return exit code 2 when daemon not running (prerequisite for the tollens upgrade alias)
- **Alias creation**: `a add tup "tollens stop" --save was_running --always "cargo build --release" --and "mdrcp" --if-saved was_running=0 "tollens start"` (after tollens change)
