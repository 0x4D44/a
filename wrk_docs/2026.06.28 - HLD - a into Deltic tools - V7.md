# HLD: Move `a` into Deltic tools

Status: DRAFT V7
Date: 2026-06-28

## Intent

Move `a` into the Deltic source tree as a Deltic-built and Deltic-installed tool, while preserving the short local command:

```text
a <alias> [args...]
```

`a` remains the fast alias CLI. Deltic owns the tool's build, install, release pins, and alias-config replication. The first build is deliberately incremental: one usable `a` tool in the Deltic workspace, one Deltic-owned alias config path, explicit native-host writer roles, manual sync, and explicit materialization to read-only target copies.

## Success Looks Like

- Users still type `a` for alias execution.
- Native Deltic host users can edit aliases with `a --add`, `a --remove`, and related management commands.
- Materialized target copies can execute aliases but cannot create hidden competing authorities.
- The Deltic repo builds and tests the `a` binary.
- Deltic installs and verifies `a` through the existing fleet-tool catalog.
- Alias config no longer depends on the standalone `0x4D44/a` repo or `a`'s hardcoded GitHub sync.
- Deltic can manually push/pull a validated alias JSON through the user's personal sync repo and materialize it to local fleet targets.

## Scope

In scope for the first build:

- Convert Deltic to a minimal workspace while keeping the current Deltic package at the repo root.
- Add `tools/a` as a separate executable package with binary name `a`.
- Add a small `crates/deltic-aliases` crate for alias schema, serde, validation, and pure path helpers.
- Move the `a` CLI implementation into `tools/a`.
- Capture a pre-port behavior oracle from the current standalone `a` before moving behavior.
- Preserve `a`'s alias execution behavior:
  - `std::process::Command`, not shell interpolation.
  - Existing `shell_words` tokenization semantics.
  - Existing parameter substitution semantics, with explicit golden tests.
  - Platform overrides.
  - Sequential and parallel chain semantics, including existing exit-code quirks unless intentionally changed with tests.
  - Saved exit-code conditions.
- Add `a` to Deltic's existing fleet tool catalog.
- Add manual Deltic alias sync commands:
  - `deltic aliases status`
  - `deltic aliases pull`
  - `deltic aliases push`
  - `deltic aliases materialize`
- Define precise acceptance criteria for the build run.

Implementation should land in this order:

1. Workspace, `deltic-aliases`, and the `tools/a` port with current behavior preserved.
2. Deltic alias status/sync/materialization commands, still manual only.
3. Fleet tool catalog, release assets, and manifest-generation support for distributing `a`.

Each stage must be green before the next stage starts. The HLD covers all three because the boundary between `a`, alias replication, and fleet rollout is the actual architectural decision.

North-star layout, not required in this first build:

```text
deltic/
  Cargo.toml
  crates/
    deltic-core/
    deltic-fleet/
    deltic-config/
  apps/
    deltic/
  tools/
    a/
    mdkloc/
    mdemail/
    mdminder/
```

The first build leaves the existing Deltic package at repo root to avoid unnecessary path churn. Moving the Deltic app to `apps/deltic` is a later workspace-cleanup requirement, not part of this HLD's implementation scope.

## Non-Goals

- No old-config migration from `~/.alias-mgr/config.json`.
- No old source-history migration.
- No rollback support.
- No staged ops choreography.
- No new environment variables.
- No feature flags.
- No change from `a` to `deltic a` for normal alias execution.
- No daemon auto-sync for aliases in the first build.
- No watcher-driven auto-push for alias config in the first build.
- No shared fleet alias layer in `deltic-config`; aliases are user-owned executable config.
- No semantic conflict resolver for concurrent native-host edits. The first build detects dirty, remote-changed, first-sync-ambiguous, or target-diverged state and refuses unsafe overwrite.

## Current Context

Current `a` facts:

- `/Users/md/language/a/Cargo.toml` is a single Rust binary crate named `a`, currently version `1.6.1`.
- Most behavior lives in `/Users/md/language/a/src/main.rs`.
- `a` dispatches management flags such as `--add`, `--remove`, `--list`, `--which`, `--export`, `--push`, and `--pull`; any other first argument is an alias name.
- Runtime config is currently `$HOME/.alias-mgr/config.json` or `%USERPROFILE%\.alias-mgr\config.json`.
- Sync is hardcoded to GitHub repo `0x4d44/a`, branch `main`, path `config.json`.
- Execution uses `std::process::Command` with inherited stdio and does not invoke an implicit shell.

Current Deltic facts:

- `/Users/md/language/deltic/Cargo.toml` is currently a single package, not a workspace.
- Deltic already distributes helper tools through `/Users/md/language/deltic/src/fleet/tools.rs`.
- Tool rollout is content-pinned by `fleet/tools.toml` in `deltic-config`.
- Current tool pin validation requires `linux_sha256` for any tool with `windows_only: false`.
- Deltic's current release scripts parse the Deltic version from the root `Cargo.toml`.
- Deltic's existing `ConfigSyncService` is phrase/settings shaped; alias sync must not reuse it directly.

## Design

### 1. Minimal Workspace Shape

Use a root package plus workspace shape for the first build:

```toml
[package]
name = "deltic"
...

[workspace]
resolver = "3"
members = [
  ".",
  "crates/deltic-aliases",
  "tools/a",
]
```

This keeps existing Deltic source paths stable while adding first-class workspace packages for `a` and the shared alias model.

`crates/deltic-aliases` is intentionally narrow. It owns:

- Alias schema.
- Serde.
- Validation.
- Pure helpers such as `aliases_path_for_home(home)`.
- Canonical JSON rendering for hashing and sync.
- Golden fixtures used by both Deltic and `a` tests.

It does not own:

- Fleet target discovery.
- GitHub sync.
- Atomic writes.
- Command execution.
- User-facing CLI text.

Those belong to the binaries that perform them.

### 2. Package Responsibilities

`tools/a` owns:

- The `a` executable.
- Alias reads on every target.
- Native-host alias writes for V1, gated by a Deltic-installed runtime context.
- Command-spec parsing for `--add`, `--platform-add`, and chain operators.
- Alias execution.
- Atomic writes to the host alias file.
- Command runner abstraction and tests.
- User-facing help and concise errors.

The Deltic root package owns:

- Fleet install and verify for the `a` binary.
- Manual alias push/pull/status/materialize commands.
- Alias sync metadata.
- Native-host-to-target materialization.
- Release packaging for the `a` assets.
- PATH-shadow detection for installed `a`.

### 3. Local Alias Config

Alias config moves from the standalone `a` directory to the Deltic config directory:

```text
Windows native host:   %USERPROFILE%\.deltic\aliases.json
WSL/Linux target:      $HOME/.deltic/aliases.json
macOS source-built target, no V1 fleet asset: $HOME/.deltic/aliases.json
```

`a --config` prints the Deltic-owned path for the current environment.

First run creates an empty alias config only when a native-host write command needs it.

All ordinary commands, including writes, fail on malformed existing config. A malformed file must never be replaced by defaults or by a partially loaded edit. Repair/reset can be a later explicit command.

The old `.alias-mgr/config.json` path is ignored. Tests must prove that an old file alone is not read, migrated, or rewritten.

### 4. Writer Roles And Target Copies

The first build defines two installed roles:

```text
NativeWriter       a Deltic native host may edit its own local aliases.json
MaterializedTarget a fleet target may read/execute a materialized aliases.json
```

This is intentionally multi-writer at the native-host layer. If Arthur edits aliases on two native Deltic hosts, Deltic does not merge the semantic changes. It uses alias sync metadata and GitHub content-SHA preconditions to detect remote movement and refuse unsafe overwrite.

Native-writer `a` supports:

- `--add`
- `--remove`
- `--platform-add`
- `--platform-remove`
- `--export`

Materialized-target `a` supports read/execution commands:

- `--help`
- `--version`
- `--config`
- `--list`
- `--which`
- `--export`
- alias execution

Materialized-target write commands fail clearly:

```text
Alias edits are Deltic-native-host authoritative in this build. Edit on a native host, then run `deltic aliases materialize`.
```

This avoids accidental unrecoverable WSL edit state. Target copies are read-mostly materialized files in the normal Deltic-managed path, not competing authorities.

`a` determines its write role through an internal `AliasRuntimeContext` provider:

- Deltic writes `%USERPROFILE%\.deltic\a_runtime_context.json` for the native Windows host with role `NativeWriter`.
- Deltic writes `$HOME/.deltic/a_runtime_context.json` for WSL/Linux materialized targets with role `MaterializedTarget`.
- macOS has no V1 fleet asset; a source-built macOS `a` without a Deltic runtime context defaults to `MaterializedTarget`.
- A missing, malformed, or untrusted runtime context defaults to `MaterializedTarget`; writes fail closed.
- Tests inject both roles without relying on real WSL, macOS, or fleet state.

The runtime context is local machine state written by Deltic install/materialization logic. It is a local role marker and accidental-use guard, not a tamper-proof security boundary. It is not read from environment variables, alias JSON, remote alias JSON, or CLI arguments. Platform identity and write authority are separate concepts: WSL uses the `linux` platform override for execution, but it is still a materialized target unless Deltic installs a native-writer context.

Runtime context schema:

```json
{
  "schema": 1,
  "role": "native_writer",
  "host_id": "<deltic presence machine_id>",
  "installed_by": "deltic"
}
```

Allowed roles are `native_writer` and `materialized_target`. The file carries no token, remote URL, command body, or alias JSON. `a` accepts `native_writer` only when the context file is under the same Deltic config directory as the alias file and has a recognized schema/role. Any other value is read-only. Same-user tampering with this local marker is out of V1's security boundary: a user who can edit the marker can also edit `aliases.json` directly. The invariant is that Deltic-installed materialized targets do not write by accident.

Runtime context lifecycle:

- The fleet tool placement path writes the native-host `a_runtime_context.json` immediately after placing or verifying the native `a` binary.
- `deltic aliases materialize` writes `materialized_target` context files alongside target `aliases.json` files.
- `deltic aliases status` verifies the native-host context and reports materialized target context state in `targets[]`.
- `a` remains `post_install: None`; the marker is not a tool post-install script. It is handled by Deltic's `a`-specific fleet/status/materialize logic.

`deltic aliases status` still scans target copies. If any target copy differs from the current native host file, status reports target divergence. `deltic aliases materialize` is the only command that overwrites target copies from the validated native host file, and it does not touch the remote.

Sync metadata:

- Use alias-specific sync metadata, not the phrase sync state's single remote SHA.
- Store at `%USERPROFILE%\.deltic\alias_sync_state.json` on each native Windows host.
- Minimal V1 fields:
  - `remote_sha`
  - `last_synced_local_hash`
  - `last_sync_at`
- Resolve login live from the authenticated Deltic personal-sync identity.
- Reuse global rate-limit state only if it is host-independent; otherwise store alias-specific rate-limit expiry.

### 5. Platform Names And Runtime Mapping

Persisted platform keys are exactly:

- `windows`
- `macos`
- `linux`

`current` is accepted only as CLI input for `--platform-add` / `--platform-remove`; it is resolved immediately before persistence.

Runtime mapping:

- `cfg(windows)` => `windows`
- `cfg(target_os = "macos")` => `macos`
- all other supported Unix/Linux builds, including WSL => `linux`

Invalid persisted platform keys fail to load. Invalid CLI platform names fail before any config write.

### 6. Canonical Hashing

Dirty-state hashing uses canonical alias JSON, not raw file bytes.

Algorithm:

1. Read bytes.
2. Parse as alias config.
3. Validate schema.
4. Serialize with deterministic key order and stable formatting through `crates/deltic-aliases`.
5. Hash the canonical bytes with SHA-256 hex.

Whitespace-only changes and object-key reordering do not create dirty state. Semantic changes do.

The canonical bytes are also the upload payload for `deltic aliases push` and the materialization payload for target copies.

### 7. Remote Alias Config

Aliases are user-owned executable configuration, not shared fleet prompt/config data. The remote source of truth uses Deltic's personal sync model:

```text
<login>/deltic-personal:config/<login>/aliases.json
```

`<login>` must come from Deltic's authenticated personal-sync identity. It must not come from alias config, environment overrides, the fleet config repo, or user-supplied command arguments.

Validate login before path construction using the GitHub login grammar Deltic already accepts for personal sync. Reject empty, slash-containing, dot-dot, non-normalized, or otherwise path-like values before building `config/<login>/aliases.json`.

Pull/push reject owner/path mismatch before reading or writing executable alias config.

Implement a separate `AliasSyncService`. It may reuse these Deltic primitives:

- token resolution
- GitHub REST/GraphQL helpers
- retry classification
- rate-limit persistence
- JSON output helpers

It must not reuse phrase/TOML-specific `ConfigSyncService` behavior such as phrase shrink guards, `AppConfig` validation, or settings/phrases path projection.

### 8. `a` CLI Behavior

Preserve:

- `a --help`, `a -h`, `a --help --examples`, and `a --help --no-examples`
- `a --version` and `a -v`
- `a --config`
- `a --export [path]`
- `a --add <name> <command> [options]`
- `a --platform-add <name> <platform> <command> [options]`
- `a --platform-remove <name> <platform>`
- `a --list [filter]`
- `a --remove <name>`
- `a --which <name>`
- `a <alias> [args...]`

Preserved add/options include:

- `--desc`
- `--force`
- chain operators: `--chain`, `--and`, `--or`, `--always`, `--if-code`, `--if-saved`, `--save`, `--parallel`

Deprecated sync flags:

- `a --push`
- `a --pull`

These no longer perform GitHub sync from inside `a`. They are omitted from help, parse before loading the alias store, ignore any legacy sync-only option such as `--message`, exit nonzero, and print:

```text
Alias sync is managed by Deltic. Use `deltic aliases push` or `deltic aliases pull`.
```

Early parsing matters: a malformed `aliases.json` must not prevent `a --push` or `a --pull` from reaching this guidance.

### 9. Alias Execution

Alias execution remains local and offline.

Before the port moves behavior into Deltic, capture a behavior oracle from the current standalone `a` at commit `d53c9e0ecbb2d455ac1af9dbac3ba22165f30e96`. The oracle must record fixture config, CLI args, runner calls, skipped commands, saved exit-code state, stdout/stderr assertions where useful, and final exit status. Port tests then assert against that captured oracle rather than redefining "current behavior" after the move.

Execution flow:

1. Load local `aliases.json`.
2. Resolve the active command for the current platform.
3. Apply existing parameter substitution semantics.
4. Split command text with `shell_words`.
5. Execute using `std::process::Command`.
6. Preserve the current simple-command child exit-code behavior.
7. Preserve current sequential and parallel chain behavior, including any existing nonzero-chain quirks unless the build explicitly changes them with tests.

Intentional V1 execution change:

- On Windows, implicit execution of `.cmd` and `.bat` programs is rejected unless the stored alias explicitly names `cmd`, `cmd.exe`, `powershell`, `powershell.exe`, `pwsh`, or another explicit shell as the program.

No alias execution path may call:

- GitHub.
- Deltic sync.
- Deltic fleet install.
- A shell, unless the stored alias explicitly names one as the program.

This is the only execution-behavior change V1 deliberately takes for security. Windows `.cmd` and `.bat` wrappers can introduce shell parsing through OS behavior, so they must be explicit shell aliases rather than accidental program resolution.

The HLD does not claim that parameter substitution preserves argv boundaries today. It requires golden tests that document the current behavior for arguments with spaces, quotes, semicolons, pipes, and leading dashes. If the build changes that behavior, the change must be explicit and covered by tests.

### 10. Manual Deltic Alias Commands

`deltic aliases status`:

- Reads host alias file.
- Reads alias sync metadata.
- Optionally probes remote status when credentials are available.
- Scans materialized target files.
- Emits human output and JSON.

Status output is structured, not a single overloaded state.

Top-level JSON fields:

```text
local_state       Missing | Malformed | Valid
sync_state        Unsynced | UpToDate | DirtyLocal | RemoteChanged | RemoteUnavailable
remote_state      Unknown | Missing | Present | Unavailable
target_state      NoTargets | UpToDate | DivergedTargets | TargetErrors
context_state     NativeWriter | MaterializedTarget | Missing | Malformed
ready_to_push     boolean
ready_to_pull     boolean
ready_to_materialize boolean
local_path
local_hash
remote_sha
remote_path
remote_login
targets[]
```

Target states:

```text
missing
equal
diverged_valid
malformed
unreadable
untrusted_path
```

Each `targets[]` entry includes `context_state` for the materialized target runtime marker.

Precedence:

- `local_state = Malformed` makes push, pull, and materialize fail before remote mutation.
- `sync_state = RemoteChanged` makes push fail before upload.
- `sync_state = DirtyLocal` makes pull fail before overwrite.
- `sync_state = Unsynced` with no `last_synced_local_hash` may pull into a missing or canonical-empty local file.
- `sync_state = Unsynced` with no `last_synced_local_hash` may adopt a baseline without rewriting aliases only when local and remote canonical hashes are equal.
- `sync_state = Unsynced` with no `last_synced_local_hash` must refuse pull when local and remote valid non-empty content differs.
- `target_state = DivergedTargets` or `TargetErrors` does not block native-host push/pull in V7 because materialized targets are not authorities.
- `ready_to_materialize = true` when at least one trusted target currently reads as missing, diverged, malformed, or unreadable.
- `ready_to_materialize = false` when there are no trusted target changes. Untrusted target paths are reported and skipped; they do not make materialization ready because Deltic must never write to them.

Readiness booleans:

```text
Case                                      ready_to_push ready_to_pull ready_to_materialize
Malformed local                          false         false         false
No credentials / remote unavailable      false         false         per target state
First push: remote missing, no metadata, valid local file true false per target state
First push: remote missing, no metadata, missing local file false false per target state
Remote deleted after prior sync          false         false         per target state
First sync: local missing/empty, remote   false         true          per target state
First sync: local == remote               false         true          per target state
First sync: local != remote               false         false         per target state
Up to date                               false         false         per target state
Dirty local only                         true          false         per target state
Remote changed only                      false         true          per target state
Dirty local and remote changed           false         false         per target state
Untrusted targets only                   per sync      per sync      false
Trusted target drift                     per sync      per sync      true
```

`per sync` means the push/pull readiness is determined solely by the sync rows above; target drift does not block sync because materialized targets are not authorities.

`deltic aliases pull`:

- Resolves authenticated personal identity.
- Downloads remote `config/<login>/aliases.json`.
- Parses and validates JSON before any write.
- Computes canonical remote hash.
- Refuses if host file is malformed.
- Refuses if host file is dirty relative to `last_synced_local_hash`.
- If sync metadata is missing and the local alias file is missing or canonical-empty, atomically writes the remote content to the native host file and records the baseline.
- If sync metadata is missing and the local alias file contains valid non-empty data canonically equal to the remote content, writes no alias file and adopts the baseline by recording `remote_sha` and `last_synced_local_hash`.
- If sync metadata is missing and the local alias file contains valid non-empty data different from the remote content, refuses with a first-sync ambiguity error and writes nothing.
- When it writes alias content, it writes the native host file atomically.
- Does not write target copies.
- Updates alias sync metadata with the remote SHA and canonical local hash.

`deltic aliases push`:

- Resolves authenticated personal identity.
- Parses and validates host `aliases.json`.
- Computes canonical host hash.
- Fetches current remote SHA before upload.
- Refuses when current remote SHA differs from metadata `remote_sha`.
- Refuses when the local host alias file is missing; push does not synthesize an empty local config.
- Allows first push only when metadata has no `remote_sha`, the local host alias file exists and validates, and the remote file is missing.
- If metadata has `remote_sha` but the remote file is missing, treats it as remote deletion and refuses without uploading.
- Uploads canonical host JSON to `config/<login>/aliases.json`.
- Requires GitHub's content-SHA conditional update/precondition on every update.
- If the remote changes between fetch and upload, refuses and leaves alias sync metadata unchanged.
- Updates alias sync metadata after successful upload.

`deltic aliases materialize`:

- Parses and validates the host file.
- Writes canonical host JSON to trusted target config dirs.
- Does not touch the remote.
- Does not update alias sync metadata.
- Exits nonzero if any trusted target write fails.
- Keeps successful per-target writes; each target write is atomic at the file level.
- Reports failed targets in the command result. Later `status` re-derives target state from the filesystem; it does not persist materialization failure history.

No alias command auto-pushes external file edits in the first build. File watchers may mark alias state dirty later, but remote mutation requires `deltic aliases push` or a future explicit design.

### 11. Target Path Trust

Deltic materializes aliases and runtime-context markers only to trusted targets discovered by its existing fleet target discovery.

For each target:

- The alias destination is exactly `<target-home>/.deltic/aliases.json`.
- The runtime-context destination is exactly `<target-home>/.deltic/a_runtime_context.json`.
- The target home itself is canonicalized before constructing the destination.
- The parent directory is created if needed, then canonicalized.
- Both final paths must remain under the expected `<target-home>/.deltic` directory.
- Symlink/reparse-point target homes and final paths are rejected.
- Traversal-like or malformed target homes are rejected.
- The trusted writer revalidates the canonical parent immediately before replacement.
- Same-user symlink races after revalidation are out of V1's threat model; V1 must still handle normal WSL target homes discovered by Deltic.
- Writes use same-directory temp files and atomic replacement where supported.

Untrusted target paths are reported in `targets[]` and skipped. They never receive alias content.

### 12. Fleet Tool Distribution

`a` joins the existing fleet tool catalog.

Catalog entry:

```rust
ToolSpec {
    name: "a",
    repo: "0x4D44/deltic",
    post_install: None,
    windows_only: false,
}
```

Ordering:

- `a` is an inert tool.
- It appears before `mdminder`, because `mdminder` has `post_install: Some("install-hooks")` and must remain last.

Current Deltic validation requires a Linux SHA for any non-Windows-only tool. Therefore the first build must produce Windows and Linux `a` assets before pinning `a` for rollout.

Release path:

- Windows release job builds the full Deltic release and `tools/a`.
- Linux release job builds only `-p a -p deltic-aliases`; it must not run the full Deltic workspace because Deltic is not a Linux product target today.
- Both jobs upload raw `a` assets and `.sha256` sidecars as workflow artifacts.
- The final publish job downloads both OS artifacts and attaches all required assets to the GitHub release.
- The publish guard fails if either required `a` sidecar is missing when `a` is in the catalog.

Release assets:

```text
a-v<version>-windows-x86_64.exe
a-v<version>-windows-x86_64.exe.sha256
a-v<version>-linux-x86_64
a-v<version>-linux-x86_64.sha256
```

Optional later:

```text
a-v<version>-macos-arm64
a-v<version>-macos-arm64.sha256
```

macOS remains `NoSource` until a macOS `a` asset and pin exist. A source-built macOS `a` without a Deltic native-writer runtime context is a materialized target in V1 and must reject write commands.

The first implementation co-versions `a` with the Deltic release tag.

Co-version rule:

- `tools/a/Cargo.toml` version equals root Deltic `Cargo.toml` version.
- `a --version` reads from package metadata, not a hand-maintained string constant.
- Release scripts fail if the `a` package version differs from Deltic's root package version.

Fleet status should also detect command shadowing:

- Windows: `where a`
- WSL/macOS: `command -v a`

If the installed Deltic-managed `a` is not the first resolved command, fleet status reports `Shadowed` with the winning path. `Shadowed` is non-green for `a` rollout verification because the user's shell would still run the standalone tool with the old config/sync behavior. Install can still place the tool, but status must not claim the user shell will run it.

### 13. File-Level Impact Map

Deltic source repo:

- `/Users/md/language/deltic/Cargo.toml`
  - Keep `[package]` for Deltic.
  - Add `[workspace]` with `resolver = "3"` and members `.`, `crates/deltic-aliases`, and `tools/a`.
- `/Users/md/language/deltic/Cargo.lock`
  - Regenerate as the workspace lockfile.
- `/Users/md/language/deltic/crates/deltic-aliases/Cargo.toml`
  - New shared alias crate.
- `/Users/md/language/deltic/crates/deltic-aliases/src/**`
  - Alias schema, serde, validation, canonical JSON, pure path helpers, and golden fixtures.
- `/Users/md/language/deltic/tools/a/Cargo.toml`
  - New tool package with binary name `a`.
- `/Users/md/language/deltic/tools/a/src/**`
  - CLI parsing, local store, `AliasRuntimeContext`, atomic native-host writes, user output, and execution.
- `/Users/md/language/deltic/tools/a/tests/**`
  - CLI and execution smoke tests.
- `/Users/md/language/deltic/src/cli.rs`
  - Add `deltic aliases status|pull|push|materialize`.
- `/Users/md/language/deltic/src/config/**` or `/Users/md/language/deltic/src/aliases/**`
  - Add `AliasSyncService`, alias sync metadata, status model, runtime-context writing, and materialization logic.
- `/Users/md/language/deltic/src/fleet/tools.rs`
  - Add `a` to `TOOLS`.
  - Add catalog, install/status, native runtime-context placement, and shadowing tests.
- `/Users/md/language/deltic/scripts/prerelease.ps1`
  - Run workspace-aware format, clippy, build, and tests.
- `/Users/md/language/deltic/scripts/package-release.ps1`
  - Emit raw Windows `a` fleet-tool assets and `.sha256` sidecars.
- `/Users/md/language/deltic/.github/workflows/release.yml`
  - Add Linux `a`-only build job.
  - Merge Windows and Linux `a` artifacts into the final publish job.
  - Keep macOS `a` assets out of the required V1 path unless a macOS builder is added.
- `/Users/md/language/deltic/scripts/release.ps1`
  - Verify Deltic and `a` package versions match before tagging.
- `/Users/md/language/deltic/scripts/release-local.ps1`
  - Verify Deltic and `a` package versions match before packaging.
- `/Users/md/language/deltic/scripts/bundle-macos.sh`
  - No V1 `a` behavior required unless macOS `a` packaging is added later.

Deltic config repo:

- `/Users/md/language/deltic-config/scripts/gen-tools-manifest.ps1`
  - Add an explicit `-AVersion` parameter.
  - Read Windows and Linux `a` sidecars from the Deltic release.
  - Regenerate `a` with mdminder/mdemail instead of dropping it on the next run.
- `/Users/md/language/deltic-config/fleet/tools.toml`
  - Generated pin entry for `a`.
- `/Users/md/language/deltic-config/tests` or script-level test equivalent
  - Golden/generated-output test proving `a` remains in the generated manifest.

Old `a` repo:

- `/Users/md/language/a`
  - No implementation changes are required by this HLD.
  - The old repo can be retired after the Deltic build lands and the fleet is using the Deltic-built `a`.

## Acceptance Criteria And Test Oracles

Workspace:

- `cargo metadata --workspace` lists `deltic`, `a`, and `deltic-aliases`.
- Root `Cargo.toml` keeps the Deltic `[package]` version.
- Workspace uses `resolver = "3"`.
- `cargo fmt --all -- --check` passes.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes on supported Deltic host targets.
- `cargo test --workspace --all-targets --all-features` passes with stdin closed on supported Deltic host targets.
- Linux CI for the `a` asset runs only `cargo test -p a -p deltic-aliases` and `cargo build --release -p a`.
- Crate graph check:
  - `tools/a` depends on `deltic-aliases`.
  - `tools/a` does not depend on Deltic sync, fleet, or GitHub modules.
  - `deltic-aliases` contains no command execution.

Versioning:

- `tools/a/Cargo.toml` version equals root Deltic package version.
- `a --version` reports that same version from package metadata.
- Release scripts fail on version drift.

Alias schema:

- Golden JSON fixtures round-trip without semantic change.
- Canonical JSON rendering is deterministic.
- Persisted platform keys are limited to `windows`, `macos`, and `linux`.
- CLI input accepts `windows`, `macos`, `linux`, and `current`.
- `current` is resolved before persistence and is never written as a platform key.
- Runtime platform mapping is tested for Windows, macOS, Linux, and WSL-as-Linux through an injected platform provider.
- Invalid platform names fail to load or fail before write, depending on whether they came from persisted JSON or CLI input.
- Reserved alias names are rejected.
- Simple aliases, platform variants, chains, parallel chains, and saved exit-code conditions serialize and deserialize correctly.

Local config and writer roles:

- Native-writer `a --config` prints the Deltic-owned `aliases.json` path, not `.alias-mgr`.
- With only old `.alias-mgr/config.json` present, `a --list` and `a <alias>` do not read it.
- With only old `.alias-mgr/config.json` present, native-writer `a --add` writes only `.deltic/aliases.json`.
- Native-writer `a --add` creates parent directories and writes valid JSON under the Deltic config dir.
- The `AliasRuntimeContext` role provider is injectable in tests.
- A valid `native_writer` runtime context under the same Deltic config dir as `aliases.json` permits write commands.
- Missing, malformed, wrong-schema, wrong-role, or wrong-directory runtime context files reject write commands.
- Fleet install/tool placement writes or refreshes native-host `a_runtime_context.json` with role `native_writer` immediately after placing or verifying the native `a` binary.
- Fleet status verifies native-host `a_runtime_context.json` and reports non-green status when the native binary is present but the context is missing or malformed.
- Deltic writes `a_runtime_context.json` with role `native_writer` for the native host target and `materialized_target` for WSL/Linux targets.
- Source-built macOS without a Deltic runtime context rejects write commands.
- No write-role decision depends on environment variables, alias JSON, remote alias JSON, or CLI arguments.
- A forged same-user `native_writer` marker is documented as out of V1's security boundary, not treated as a rejected security case.
- Materialized-target `a --add`, `--remove`, `--platform-add`, and `--platform-remove` exit nonzero with the Deltic-native-host-authoritative edit message.
- Config writes are atomic: a simulated write failure leaves the previous file intact.
- Malformed local config makes read, write, and execution commands fail with a nonzero exit.
- Read-only commands never replace malformed config with defaults.

CLI behavior:

- Known management flags dispatch to management handlers.
- Unknown `--` flags fail with usage.
- Bare first arguments dispatch to alias execution.
- `a --help` omits `--push` and `--pull`.
- `a --push` and `a --pull` parse before loading the alias store, exit nonzero, print the Deltic handoff message, and invoke no sync backend.
- `a --export [path]` exports the new `.deltic/aliases.json` content and never reads `.alias-mgr`.
- `a --list` remains sorted and filterable.
- `a --which` shows default command, active platform command, platform variants, and chain details.
- A command matrix covers every preserved management flag with initial JSON, command, expected JSON, exit code, and key output.

Execution:

- Pre-port oracle capture from standalone `a` commit `d53c9e0ecbb2d455ac1af9dbac3ba22165f30e96` is checked into the build branch before the port changes behavior.
- Port tests compare against the pre-port oracle rather than newly invented goldens.
- Golden behavior tables cover:
  - alias fixture
  - CLI args
  - expected runner calls
  - expected skipped commands
  - saved-code state
  - final process exit status
- Golden cases include simple command success/failure, sequential chain skip/continue, parallel nonzero-child behavior, saved-code conditions, substitution, and platform override.
- A mock runner proves no extra shell process is inserted for commands containing `&&`, `||`, `;`, pipes, or quoted arguments.
- Windows `.cmd` / `.bat` program resolution is rejected unless the stored alias explicitly names `cmd`, `cmd.exe`, `powershell`, `powershell.exe`, `pwsh`, or another explicit shell as the program.
- Windows resolver tests cover literal `foo.cmd` / `foo.bat`, extensionless `foo` resolving through `PATHEXT`, relative paths, absolute paths, and case-varied `.CMD` / `.BAT`.
- Explicit-shell allowlist tests prove `cmd /c foo.cmd`, `powershell -File foo.ps1`, and `pwsh -File foo.ps1` remain allowed because the shell is explicitly named.
- The pre-port oracle may show old implicit wrapper behavior, but the port must record this as the single intentional execution behavior change.
- An injection oracle such as `rec one; touch sentinel` invokes `rec` with literal args and does not create `sentinel`.
- Parameter substitution covers `$1`, multi-digit arguments, `$@`, `$*`, `$$`, missing args, quoted args, spaces, semicolons, pipes, and leading dashes.
- Sequential chains preserve current `&&`, `||`, `;`, `--if-code`, `--save`, and `--if-saved` behavior, including current final-exit behavior.
- Parallel chains preserve current success and failure aggregation, including current nonzero-child behavior.
- Platform override tests prove the current platform command wins over the default.
- Execution tests pass with network disabled and without any Deltic sync dependency.

Deltic alias sync:

- `AliasSyncService` tests use a fake remote, fake authenticated login, fake target inventory, temp target stores, and injected clock.
- `deltic aliases status --json` exposes:
  - `local_state`
  - `sync_state`
  - `remote_state`
  - `target_state`
  - `context_state`
  - `ready_to_push`
  - `ready_to_pull`
  - `ready_to_materialize`
  - `local_path`
  - `local_hash`
  - `remote_sha`
  - `remote_path`
  - `remote_login`
  - `targets[]` with path, hash, state, and context state
- Status truth-table tests cover missing, malformed, valid, dirty local, remote changed, remote unavailable, equal targets, diverged valid targets, malformed targets, unreadable targets, and untrusted target paths.
- Status readiness truth-table tests assert `ready_to_push`, `ready_to_pull`, and `ready_to_materialize` for malformed local, no credentials, remote unavailable, first-push remote missing with valid local, first-push remote missing with missing local, remote deleted after prior sync, first-sync missing local, first-sync empty local, first-sync equal local/remote, first-sync differing local/remote, dirty local, remote changed, untrusted-only targets, and trusted diverged targets.
- Canonical hash tests prove whitespace-only changes and object-key reorder do not create dirty state, while semantic changes do.
- `deltic aliases pull` validates remote JSON before replacing any local file.
- `deltic aliases pull` refuses dirty local native-host state.
- With no alias sync metadata and a missing or canonical-empty local alias file, `deltic aliases pull` seeds the local file from remote and records the baseline.
- With no alias sync metadata and canonically equal local and remote alias JSON, `deltic aliases pull` writes no alias content and adopts the baseline by recording `remote_sha` and `last_synced_local_hash`.
- With no alias sync metadata and differing valid local and remote alias JSON, `deltic aliases pull` refuses with a first-sync ambiguity error and writes nothing.
- `deltic aliases pull` writes only the native host alias file and alias sync metadata.
- `deltic aliases pull` does not write target copies.
- `deltic aliases push` fetches current remote SHA before upload.
- `deltic aliases push` refuses stale metadata when remote has changed.
- `deltic aliases push` requires the GitHub content-SHA precondition on every update.
- A fake-remote race test changes the remote between fetch and upload; push must fail and leave sync metadata unchanged.
- First push succeeds only when metadata has no `remote_sha`, the local host alias file exists and validates, and the remote file is missing.
- Missing local host alias file is not push-ready and push does not synthesize canonical empty JSON.
- If metadata has `remote_sha` but the remote file is missing, push treats that as remote deletion and refuses without uploading.
- `deltic aliases push` uploads only canonical validated host JSON.
- `deltic aliases materialize` writes validated host JSON to trusted target stores and does not touch remote.
- `deltic aliases materialize` writes `materialized_target` runtime context files alongside target alias files.
- `deltic aliases materialize` does not update alias sync metadata.
- `deltic aliases materialize` exits nonzero if any trusted target write fails, while preserving successful per-target atomic writes.
- The `materialize --json` result reports failed targets for that command invocation.
- Later `status --json` derives target state from current filesystem reads and does not rely on persisted materialization failure history.
- Later `status --json` keeps `ready_to_materialize = true` when trusted target copies are still missing, diverged, malformed, or unreadable.
- `status --json` reports native and target runtime context state.
- The remote path is `config/<login>/aliases.json` in the authenticated user's `deltic-personal` repo.
- Owner/login mismatch is rejected before pull or push.
- Login grammar tests reject slash, dot-dot, empty, and non-normalized login values before remote path construction.
- No code path references `0x4d44/a` as the alias sync target.
- Alias sync metadata is independent of phrase sync's single remote SHA.
- Sync/status logs and JSON include alias names only when explicitly requested; default output uses counts, hashes, paths, and state.
- Sync/materialize/status errors may print state, path, hash, alias count, and parse line/column, but never raw JSON, command bodies, tokens, auth headers, or response bodies.

Target materialization:

- Target alias destination is exactly `<target-home>/.deltic/aliases.json`.
- Target runtime-context destination is exactly `<target-home>/.deltic/a_runtime_context.json`.
- Target home canonicalization is tested before destination construction.
- Symlink/reparse-point target homes and final paths are rejected.
- Parent directory identity is revalidated immediately before replacement.
- Path trust tests apply to both target files.
- Normal Deltic-discovered WSL home paths materialize successfully in V1 tests.
- Same-user symlink races after parent revalidation are documented as out of V1 scope.
- Paths escaping `<target-home>/.deltic` are rejected.
- Missing target file materializes successfully.
- Equal target file is a no-op.
- Diverged valid target file is reported and overwritten only by explicit `deltic aliases materialize`.
- Malformed target file is reported and overwritten only by explicit `deltic aliases materialize`.
- Unreadable target file is reported and not overwritten unless write repair succeeds through the explicit materialize command.

Fleet tool distribution:

- `src/fleet/tools.rs::TOOLS` contains `a`.
- `a` appears before hook-bearing `mdminder`.
- A valid `[[tool]] name = "a"` pin reconciles to `Missing`, `Stale`, or `UpToDate` by SHA-256.
- An absent `a` pin is `NoSource`, not an error.
- If `a` is `windows_only: false`, manifest validation requires valid Windows and Linux SHA pins.
- Install-path tests place `a.exe` under `%LOCALAPPDATA%\Programs\Deltic` on Windows and `a` under `~/.local/bin` on WSL.
- macOS is `NoSource` unless a macOS `a` asset and pin are added.
- Release packaging emits Windows and Linux raw `a` assets and matching `.sha256` sidecars before `a` is pinned for rollout.
- The generated `fleet/tools.toml` entry for `a` uses sidecar hashes, not hand-typed hashes.
- `gen-tools-manifest.ps1` golden-output test proves `a` remains in the generated manifest.
- Cross-repo validation runs the deltic-config manifest generator against fixture sidecars for `a`, mdminder, and mdemail, then diffs the generated `fleet/tools.toml` against the expected golden output.
- Fleet status reports `Shadowed` when `where a` or `command -v a` resolves a non-Deltic-managed `a` before the installed Deltic copy.
- `Shadowed` is non-green for `a` rollout verification; a green rollout requires the user shell to resolve the Deltic-managed `a` first.

## Residual Risks

- Even a minimal workspace can expose release or tooling assumptions around root-package layout.
- Alias config is executable authority. The safety boundary is no implicit shell; it is not a claim that aliases are safe.
- Windows batch wrappers can invoke shell-like parsing through OS behavior; V1 rejects implicit `.cmd` / `.bat` aliases unless an alias explicitly names a shell.
- Multi-native-host edits can still race. The first build detects dirty, remote-changed, and first-sync-ambiguous state and refuses unsafe overwrite instead of resolving conflicts.
- The runtime context file is a local role marker, not a tamper-proof security control. Same-user marker tampering is out of V1 scope.
- Same-user target path races after canonical parent revalidation are out of V1 scope.
- Materialized-target alias edits are not first-class writers in V1. Write commands fail there; users must edit on a native Deltic host and materialize.
- Co-versioning `a` with Deltic is simple but couples `a` releases to Deltic releases.
- Linux asset production is required before WSL rollout under current Deltic tool validation.
- A shadowed `a` can still exist on PATH; status detects it, but fixing PATH order remains user/environment work.

## Decision Log

- Decision: Keep `a` as a separate binary named `a`.
  - Rationale: aliases are muscle-memory tooling; `deltic a <name>` is the wrong user experience.

- Decision: Move `a` source to `tools/a`.
  - Rationale: `tools/` communicates that this is a user-facing utility owned by Deltic's release train.

- Decision: Keep the current Deltic package at repo root for the first build.
  - Rationale: it avoids broad `apps/deltic` path churn and prevents immediate breakage in release scripts that parse root `Cargo.toml`.

- Decision: Add only `crates/deltic-aliases` now.
  - Rationale: both Deltic and `a` need the same alias schema and validation. Other future crates are deferred until they remove real duplication.

- Decision: Deltic owns replication; `a` owns native-host edits and local execution.
  - Rationale: this keeps alias execution fast and offline while removing the separate `a` repo as a config-sync owner.

- Decision: Native Deltic hosts are explicit writers; materialized targets are read-only.
  - Rationale: this matches a multi-machine fleet without pretending there is one global editing box. Remote SHA preconditions and first-sync rules prevent silent clobbers.

- Decision: `a` uses a Deltic-installed `AliasRuntimeContext` file for V1 write authority.
  - Rationale: source-built or unmarked copies fail closed as materialized targets, while Deltic-installed native hosts are explicit writers. This avoids environment variables and keeps the predicate injectable in tests.

- Decision: Materialized-target `a` write commands fail in V1.
  - Rationale: allowing WSL/Linux/macOS target writes without an adoption/merge path creates unrecoverable divergence.

- Decision: `deltic aliases pull` writes only the native host alias file and sync metadata.
  - Rationale: `deltic aliases materialize` is the single explicit operation that overwrites target copies, which keeps target repair semantics clear and testable.

- Decision: Store alias sync metadata at `%USERPROFILE%\.deltic\alias_sync_state.json` on each native Windows host.
  - Rationale: one concrete path is easier to test and avoids leaving a storage fork for the builder.

- Decision: First `deltic aliases pull` refuses when local and remote valid alias data differ and no sync baseline exists.
  - Rationale: the first sync must not overwrite executable local config or choose a winner for divergent content.

- Decision: First `deltic aliases pull` may adopt equal local and remote canonical content when sync metadata is missing.
  - Rationale: equal hashes prove no content winner is being chosen, so Deltic can safely restore `remote_sha` and `last_synced_local_hash` without rewriting aliases.

- Decision: Reject implicit Windows `.cmd` / `.bat` execution in V1.
  - Rationale: preserving the no-implicit-shell boundary matters more than preserving this Windows wrapper behavior.

- Decision: Treat `a_runtime_context.json` as a local role marker, not a security boundary.
  - Rationale: this prevents accidental writes from materialized targets without adding signatures, ACL choreography, or another trust root to V1.

- Decision: Capture a pre-port behavior oracle from standalone `a` commit `d53c9e0ecbb2d455ac1af9dbac3ba22165f30e96`.
  - Rationale: preserving current behavior needs an independent oracle taken before the port, not tests written after behavior has moved.

- Decision: `deltic aliases push` must fetch and compare remote SHA before upload.
  - Rationale: executable alias config must not silently clobber another machine's remote edit.

- Decision: `deltic aliases push` does not synthesize missing local alias files.
  - Rationale: pushing should publish an explicit local alias file. A missing local file is not the same thing as intentional empty alias config.

- Decision: Remote deletion after a prior sync is not treated as first push.
  - Rationale: a missing remote with stored `remote_sha` is remote movement; Deltic refuses instead of recreating the file silently.

- Decision: Target path trust applies to both `aliases.json` and `a_runtime_context.json`.
  - Rationale: Deltic writes both files into the same target config directory, so they need the same destination and parent-validation oracle.

- Decision: Use Deltic's personal sync repo for alias data.
  - Rationale: aliases are user-owned executable config. The shared `deltic-config` repo remains the source for fleet-owned artifacts such as tool pins, prompts, and skills.

- Decision: Implement `AliasSyncService`, not direct `ConfigSyncService` reuse.
  - Rationale: phrase/settings sync is TOML and phrase-specific. Alias sync is JSON and needs its own validation and dirty-state rules.

- Decision: Remove old direct `a --push` / `a --pull` GitHub sync.
  - Rationale: the hardcoded `0x4D44/a:config.json` path is exactly the repo split this design removes. Manual sync belongs under `deltic aliases`.

- Decision: Keep `a --export`, retargeted to the new Deltic-owned alias path.
  - Rationale: export is useful local inspection and does not violate Deltic sync ownership.

- Decision: Use existing fleet tool distribution for the `a` binary.
  - Rationale: Deltic already has content-pinned install/status machinery for helper tools. A second installer would add complexity without value.

- Decision: Publish Windows and Linux `a` assets before pinning `a` as a non-Windows-only tool.
  - Rationale: current Deltic pin validation requires Linux SHA for non-Windows-only tools. This avoids a manifest entry that installs nowhere.

- Decision: Co-version `a` with Deltic at first.
  - Rationale: one release train is the point of moving these tools into Deltic. Independent tool versions can be revisited after more tools move in.

- Tie-break: Choose Draft C's replication and testability over Draft A's binary-only move.
  - Rationale: binary consolidation without alias replication misses Arthur's stated goal.

- Tie-break: Choose Draft B's Deltic-owned local path and malformed-config safety over Draft C's old `.alias-mgr` path.
  - Rationale: Deltic should own the alias lifecycle. Preserving the old path is not required in the sandbox.

- Tie-break: Scale V2/V7 down from daemon auto-sync to manual alias sync/materialization.
  - Rationale: the first build should establish correct ownership and testable sync semantics before adding watchers or periodic automation.

- Dissent recorded: one judge preferred keeping `a --push` / `a --pull` as Deltic-managed front-ends.
  - Resolution: keep the boundary clearer for V1. `a` reports the Deltic command; Deltic owns sync.
